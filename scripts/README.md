Runnable benchmark scripts live in `../benchmark/` and `../benchmark/scripts/`.

Use `../benchmark/run_all.sh` for the full single-model pipeline,
`../benchmark/run_all_models.sh` for the full model sweep, and
`../benchmark/run_single.sh` for targeted baseline, vLLM, SGLang, profiling, or
visualization runs.
