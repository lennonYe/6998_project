# Top-level package versions used for the reported L4 benchmark runs.
# The serving frameworks have conflicting transitive dependencies, so the
# README describes the three separate virtual environments used in practice.

torch==2.4.0
transformers==4.46.0
accelerate==1.0.1
bitsandbytes==0.44.1
vllm==0.6.4.post1
sglang[all]==0.4.0.post1
aiohttp==3.10.10
requests==2.32.3
matplotlib==3.9.2
wandb==0.18.5
