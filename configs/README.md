Benchmark configuration lives in `../benchmark/config.py`.

This directory is kept at the repository root for the HPML submission layout.
The active configuration file defines model choices, prompt suites, concurrency
levels, serving ports, and generation parameters used to reproduce the reported
figures.
